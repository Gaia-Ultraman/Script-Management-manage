import React, { Component } from "react";
import { Menu, icons } from "antd"
import { MobileOutlined, DatabaseOutlined,SnippetsOutlined } from '@ant-design/icons';
const { SubMenu } = Menu;

export default class ScriptConfig extends React.Component {
    state = {
        
    };

    render() {
        return (
            <div>脚本配置正在开发中,将传入设备信息分组信息及中控API，需要开发者拥有JS和HTML相关知识!将导入开发者自定义的JS文件。</div>
        );
    }
}